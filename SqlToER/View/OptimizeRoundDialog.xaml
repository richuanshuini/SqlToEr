<Window x:Class="SqlToER.View.OptimizeRoundDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="优化轮次设置" Width="360" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F6FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔄 设置本次优化轮次"
                   FontSize="15" FontWeight="SemiBold"
                   Foreground="#2D3748" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="优化轮数：" VerticalAlignment="Center"
                       FontSize="13.5" Foreground="#4A5568" />
            <TextBox x:Name="RoundsTextBox" Width="80" Height="30"
                     Text="3" FontSize="14" Padding="8,4"
                     VerticalContentAlignment="Center"
                     BorderBrush="#CBD5E0" BorderThickness="1">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6" />
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <TextBlock Text="  (每轮参数自动加强 ×0.3)" VerticalAlignment="Center"
                       FontSize="11.5" Foreground="#A0AEC0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" Width="80" Height="34"
                    FontSize="13" Cursor="Hand"
                    Background="#E2E8F0" Foreground="#4A5568"
                    BorderThickness="0"
                    Click="Cancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8" />
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="开始优化" Width="100" Height="34"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                    Background="#F59E0B" Foreground="White"
                    BorderThickness="0" Margin="10,0,0,0"
                    Click="OK_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8" />
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
